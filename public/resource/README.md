# Preview packages

Place your Sonolus packages in this directory:

- `skin.scp` (required): skin package
- `particle.scp` (optional): particle package for hit effects

The preview uses the first skin or particle item in each package.
